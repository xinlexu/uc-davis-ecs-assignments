# ECS164 Password Generator

Course project with a Flask backend and a static Vue-based frontend.

## Run

```sh
cd backend
python -m pip install -r requirements.txt
python identify.py
```

Then open `frontend/index.html` in a browser. The backend creates `identifier.sqlite` on first run; that local database is ignored by Git.
