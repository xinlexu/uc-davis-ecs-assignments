# ECS150 Project 4

Source-only distributed file system submission files extracted from the local project archive.

The course-provided framework headers and build harness were not present in the local submission archive, so this directory preserves the submitted implementation files without generated metadata.
